{
  "name": "3000-router-host",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/router-demo/3000-router-host/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npm run build --prefix apps/router-demo/3000-router-host"]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npm run test --prefix apps/router-demo/3000-router-host"]
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npm run dev --prefix apps/router-demo/3000-router-host"]
      },
      "dependsOn": [
        {
          "target": "build",
          "dependencies": true
        }
      ]
    }
  },
  "tags": ["type:app"]
}
