{
  "name": "router-host-2000",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/router-demo/router-host-2000/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npm run build --prefix apps/router-demo/router-host-2000"]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npm run test --prefix apps/router-demo/router-host-2000"]
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npm run dev --prefix apps/router-demo/router-host-2000"]
      },
      "dependsOn": [
        {
          "target": "build",
          "dependencies": true
        }
      ]
    },
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npm run e2e --prefix apps/router-demo/router-host-2000"]
      }
    },
    "test:e2e": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": true,
        "commands": [
          {
            "command": "lsof -i :3005 || nx run 3005-runtime-host:serve",
            "forwardAllArgs": false
          },
          {
            "command": "sleep 4 && nx run 3005-runtime-host:e2e",
            "forwardAllArgs": true
          }
        ]
      }
    }
  },
  "tags": ["type:app"]
}
