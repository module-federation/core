import React from 'react';
const style = {
  background: '#00c',
  color: '#fff',
  padding: 12,
};
const Button = () => (
  <button style={style}>react-ts-nested-remote Button</button>
);

export default Button;
