# name

- Tipo: `string`
- Obrigatório: Yes

`Module Federation` módulo name, o name deve ser unique.

`Module Federation` usa o name for runtime dados retrieval e global chunk storage variable references.
