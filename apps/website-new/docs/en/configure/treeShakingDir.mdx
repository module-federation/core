# treeShakingDir

The output directory for tree-shaking shared fallback assets.

- Type: `string`
- Required: No
- Default: `undefined`

When shared dependency tree shaking is enabled, Module Federation splits out the unused exports of shared modules as fallback assets. This option specifies the output directory for those fallback assets.
