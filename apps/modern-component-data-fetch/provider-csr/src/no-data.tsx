const ContentWithoutDataLoader = (): JSX.Element => {
  return (
    <div id="provider-csr-without-data-loader">
      CSR component without data loader
    </div>
  );
};

export default ContentWithoutDataLoader;
