/**
 * Node MF environment entry for the React remote.
 * Rsbuild builds this to dist/ssr/ for the host to consume via
 * bridge_ssr_react@<origin>/ssr/mf-manifest.json at request time.
 */
export {};
