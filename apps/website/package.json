{
  "name": "website",
  "scripts": {
    "build.server": "vite build -c adaptors/netlify-edge/vite.config.ts",
    "deploy": "netlify deploy"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.11"
  }
}
