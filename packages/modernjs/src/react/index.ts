export {
  ERROR_TYPE,
  CacheSize,
  CacheTime,
  configureCache,
  generateKey,
  cache,
  revalidateTag,
  clearStore,
  lazyLoadComponentPlugin,
} from '@module-federation/bridge-react';

export type {
  DataFetchParams,
  CacheStatus,
  CacheStatsInfo,
} from '@module-federation/bridge-react';
