/**
 * Type declarations for the ESM `@module-federation/node/register` entry
 * point (`register.mjs`). Importing it registers the native HTTP loader
 * hooks as a side effect; it has no exports.
 */
export {};
