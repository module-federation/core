/**
 * Type declarations for the CommonJS `@module-federation/node/register`
 * entry point (`register.cjs`). Requiring it registers the native HTTP
 * loader hooks as a side effect; it has no exports.
 */
export {};
