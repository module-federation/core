{
  "name": "native-federation-typescript",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/native-federation-typescript/src",
  "projectType": "library",
  "tags": ["type:pkg"],
  "implicitDependencies": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/packages/native-federation-typescript/dist"],
      "options": {
        "parallel": false,
        "cwd": "packages/native-federation-typescript",
        "commands": [
          "tsdown --config tsdown.config.ts",
          "cp package.json dist",
          "cp *.md dist"
        ]
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "outputs": ["{options.outputFile}"],
      "options": {
        "command": "pnpm exec eslint \"packages/native-federation-typescript/**/*.ts\"",
        "forwardAllArgs": false
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/coverage/packages/native-federation-typescript"
      ],
      "options": {
        "command": "pnpm exec vitest run --passWithNoTests --config packages/native-federation-typescript/vite.config.mts",
        "forwardAllArgs": false
      }
    },
    "pre-release": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          {
            "command": "nx run native-federation-typescript:test",
            "forwardAllArgs": false
          },
          {
            "command": "nx run native-federation-typescript:build",
            "forwardAllArgs": false
          }
        ]
      }
    }
  }
}
