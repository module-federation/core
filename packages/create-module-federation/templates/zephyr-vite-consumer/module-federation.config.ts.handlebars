const config = {
  name: '{{mfName}}',
  remotes: {
    'provider': {
      name: '{{mfName}}_provider',
      entry: 'http://localhost:3004/remoteEntry.js',
    }
  },
  shared: {
    react: {
      singleton: true,
    },
    'react-dom': {
      singleton: true,
    },
  },
};

export default config;
