import * as React from 'react';
import ComponentA from './ComponentA'
export default function App() {
  debugger;
  return `App rendered with [${React.default()}] with layered value: [${React.layeredComponentsReact()}] and ${ComponentA()}`;
}
