const fs = require('fs');

import {
  Assets,
  Chunks,
  Module,
  Origin,
  Error,
  Warning,
  Reason,
  Dependency,
  Children,
  Issuer,
} from "./types";

types = []

const queries = types.map((Class) => Class.queries
const 
const types




// The resolver map
export default {
  Query,
};
