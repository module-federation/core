---
'@module-federation/managers': minor
---

Add pure helpers that plan a composed federation runtime: `optionsParticipant`, `planComposition`, `resolveRuntimeFamily`, `selectMode`, `resolveImports`, `renderComposition`, and `checkFederationGraph`.
