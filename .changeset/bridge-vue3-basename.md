---
'@module-federation/bridge-vue3': minor
---

Add explicit `basename` to `createRemoteAppComponent` and derive host basenames from Vue Router and Nuxt-style catch-all routes (including nested matches).
