export type Source = any;
/**
 * @param {Source} a a source
 * @param {Source} b another source
 * @returns {boolean} true, when both sources are equal
 */
export function isSourceEqual(a: any, b: any): boolean;
